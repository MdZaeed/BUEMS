@model IEnumerable<BUEMS.Models.Grade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GradeRange)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GradeRange)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GradeNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.GradeNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GradeNo })
        </td>
    </tr>
}

</table>
